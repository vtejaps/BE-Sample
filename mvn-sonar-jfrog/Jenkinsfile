pipeline 
{
    agent {
            node{
        label 'ubuntu-PS'
        }
    }
    tools {
      maven 'Maven'
    }
    stage ('Build') {
            steps{
                script{
                    sh "mvn clean install"
                }
            }
    }
}
